{
  "name": "node-starter",
  "version": "0.0.0",
  "main": "index.html",
  "scripts": {
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite": "lite-server"
  },
  "dependencies": {
    "live-server": "^1.2.1"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
